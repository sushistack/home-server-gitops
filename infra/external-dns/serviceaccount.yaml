# One SA for this one actor (AR27: RBAC scales with actors, not services). The external-dns
# Deployment runs as this SA; its read-only ClusterRole (rbac.yaml) is its only cluster access.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-dns
  namespace: external-dns
