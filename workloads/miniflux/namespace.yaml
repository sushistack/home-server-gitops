# Story 4.6 — Miniflux: the ONE Postgres-backed service. app (miniflux) + db (miniflux-db) are
# ONE logical service in ONE namespace / ONE ArgoCD Application (1:1, generated by the Story 1.3
# ApplicationSet over workloads/* — do NOT hand-write an Application). Backup/restore is LOGICAL
# pg_dump (NOT a tar/volume snapshot), per architecture.md 186-187, 539.
apiVersion: v1
kind: Namespace
metadata:
  name: miniflux
